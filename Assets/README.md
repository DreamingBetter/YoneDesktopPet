# Assets

这个目录用于放置角色图片。

当前默认文件名：

```text
yone.jpg
```

仓库还包含：

```text
向左飞行.png
```

这张图片目前作为备用动作素材保存，程序主逻辑默认仍使用 `yone.jpg`。

注意：

- 角色图片可能涉及第三方版权或用户私有素材，不包含在 MIT 代码许可证内。
- 如果要替换角色，请把新图片命名为 `yone.jpg`，或同步修改 `YoneDesktopPet.csproj` 和 `MainWindow.xaml.cs` 中的资源路径。
